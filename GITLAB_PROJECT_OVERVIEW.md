# Biz Factory

面向 AI 协同的应用研发与交付体系探索。

## 项目定位

Biz Factory 不是一个单纯的前端模板，也不是把页面简单拼起来的低代码工具。

它尝试解决的是一个更底层的问题：

> 当应用数量、客户差异、交付形态和协作角色持续增加时，如何把“项目式开发”升级为“标准化生产应用”。

在传统工程模式下，页面、菜单、路由、应用入口、构建脚本和交付形态通常混在一起。短期能快速交付，但随着项目变多，会逐渐出现大工程膨胀、重复开发、差异化交付困难、研发边界不清、AI 难以准确理解上下文等问题。

Biz Factory 的目标，是把这些原本散落在工程里的关系抽象成稳定的模型和配置，让应用可以被描述、被校验、被运行、被构建，并逐步支持 AI 参与开发和交付。

## 核心思路

项目采用三层模型组织应用生产过程：

- 页面能力：应用中可复用的最小功能单元。
- 导航方案：页面能力的组织方式，描述菜单、路由和页面挂载关系。
- 业务应用：最终运行和交付的应用成品，绑定某个导航方案并声明交付形态。

这三层关系通过 manifest 进行标准化描述：

- `page.manifest.json` 描述一个页面能力是什么。
- `*.plan.json` 描述一套导航方案如何组织页面。
- `*.app.json` 描述一个应用如何运行、构建和交付。

这样做的价值不是多写几份配置，而是让应用结构从“隐含在代码里”变成“可以被程序读取的正式契约”。

## 解决的问题

### 1. 大工程治理

传统工程会随着页面和客户增加越来越重。Biz Factory 将页面能力、导航方案和业务应用拆开管理，让工程复杂度不再简单堆积在一个大入口里。

### 2. 差异化交付

不同客户或场景需要的功能范围不同。通过应用 manifest，可以按应用选择页面能力、运行入口和构建输出，减少复制工程和硬编码定制。

### 3. 多形态交付

同一套能力可以根据场景输出不同形态：

- 微应用形态：适合大型平台统一承载。
- 独立应用形态：适合轻量场景单独部署。

### 4. 研发边界收敛

应用不是只在运行时被裁剪，也可以在开发时被裁剪。项目支持基于应用配置创建开发工作区，让开发者只关注当前应用相关代码，降低大仓库协作成本。

### 5. AI 协同基础

AI 参与研发最需要清晰边界、结构化上下文和可验证闭环。manifest 将页面、方案和应用显式建模后，可以为 AI 提供更小、更准确、更可验证的任务上下文。

## 当前能力

- Manifest 驱动的页面、方案、应用配置。
- Schema 与引用关系校验。
- 按应用生成页面模块和路由树。
- 按应用运行开发服务。
- 按应用构建交付产物。
- 按应用创建开发工作区。
- 微应用入口与独立交付入口并存。

## 目录结构

```text
apps/
  itg-portal/          # 宿主基座
  uoc-portal/          # 微应用入口 / 应用工作台
  assembly-portal/     # 独立应用交付入口
  manifests/
    apps/              # 应用 manifest
    plans/             # 导航方案 manifest

packages/
  common/              # 公共能力
  page-*/              # 页面能力包

schemas/               # manifest schema

scripts/
  app-runtime/         # 共享运行、构建、工作区脚本
```

## 快速开始

环境要求：

- Node.js 20+
- pnpm 9.x

安装依赖：

```bash
pnpm install
```

校验 manifest：

```bash
pnpm validate:manifests
```

执行 P0 自检：

```bash
pnpm check:p0
```

运行默认入口：

```bash
pnpm dev
```

构建默认应用：

```bash
pnpm build
```

## 适合展示的亮点

### 应用不是写死的，而是描述出来的

应用由 manifest 描述，程序根据配置生成路由、菜单、入口和构建结果。

### 交付形态不是单一的，而是可选择的

同一套页面能力可以根据场景输出微应用或独立应用。

### 开发视角也可以按应用裁剪

不只运行和构建按应用收敛，开发工作区也可以按应用收敛，减少大仓库认知负担。

### 天然适合 AI 协作

结构化配置让 AI 可以明确知道当前应用包含哪些页面、有哪些路由、使用哪种交付形态，以及应该验证什么。

## 后续规划

### 第一阶段：稳定工程底座

- 完善 manifest schema。
- 稳定校验、运行、构建、自检流程。
- 统一脚本入口和文档。

### 第二阶段：抽象核心能力

- 抽出应用运行内核。
- 抽出 manifest 解析与生成逻辑。
- 收敛公共组件、公共服务和公共资产。

### 第三阶段：产品化应用生产台

- 页面能力管理。
- 导航方案管理。
- 应用配置管理。
- 依赖关系可视化。
- 构建与校验报告。

### 第四阶段：AI 协同开发

- AI 根据需求推荐页面组合。
- AI 生成应用 manifest。
- AI 创建应用级开发工作区。
- AI 执行构建、自检和交付验证。

## 一句话总结

Biz Factory 希望把应用研发从“人工堆项目”推进到“标准化生产应用”，并为 AI 参与开发、验证和交付提供清晰、可执行的工程边界。
